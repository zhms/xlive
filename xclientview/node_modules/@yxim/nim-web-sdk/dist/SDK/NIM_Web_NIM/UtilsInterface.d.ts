import { NIMFriendProfile } from './FriendInterface';
import { NIMMessage } from './MessageInterface';
import { NIMSession } from './SessionInterface';
import { NIMSystemMessage } from './SystemMessageInterface';
import { NIMTeam, NIMTeamMember } from './TeamInterface';
import { NIMCommonError, NIMStrAnyObj } from '../types';
import { NIMUserNameCard } from './UserInterface';
/**
 * 工具函数定义
 */
export interface UtilsInterface {
    /**
     * 日志保存到本地的方法，日后探寻下
     */
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 合并登录端
     */
    mergeLoginPorts(arr1: NIMStrAnyObj[], arr2: NIMStrAnyObj | NIMStrAnyObj[]): NIMStrAnyObj[];
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 从登录端中去除某些登录短
     */
    cutLoginPorts(arr1: NIMStrAnyObj[], arr2: NIMStrAnyObj | NIMStrAnyObj[]): NIMStrAnyObj[];
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 合并关系
     */
    mergeRelations(arr1: NIMStrAnyObj[], arr2: NIMStrAnyObj | NIMStrAnyObj[]): NIMStrAnyObj[];
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 去除关系
     */
    cutRelations(arr1: NIMStrAnyObj[], arr2: NIMStrAnyObj | NIMStrAnyObj[]): NIMStrAnyObj[];
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 在关系数组里面根据 `account` 找到对应的关系
     *
     * @param arr1  关系数组
     * @param account  待查找的关系的 `account`
     */
    findRelation(arr1: NIMStrAnyObj[], account: string): NIMStrAnyObj | null;
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 合并好友
     */
    mergeFriends(arr1: NIMFriendProfile[], arr2: NIMFriendProfile | NIMFriendProfile[]): NIMFriendProfile[];
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 去除好友
     */
    cutFriends(arr1: NIMFriendProfile[], arr2: NIMFriendProfile | NIMFriendProfile[]): NIMFriendProfile[];
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 去除`accounts`对应的好友
     */
    cutFriendsByAccounts(arr1: NIMFriendProfile[], arr2: string | string[]): NIMFriendProfile[];
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 在关系数组里面根据 `account` 找到对应的
     *
     * @param arr1  好友数组
     * @param account  待查找的 `account`
     */
    findFriend(arr1: NIMFriendProfile[], account: string): NIMFriendProfile | null;
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 合并用户名片
     */
    mergeUsers(arr1: NIMUserNameCard[], arr2: NIMUserNameCard | NIMUserNameCard[]): NIMUserNameCard[];
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 在用户名片数组里面根据 `account` 找到对应的
     *
     * @param arr1  用户名片数组
     * @param account  待查找的 `account`
     */
    findUser(arr1: NIMUserNameCard[], account: string): NIMUserNameCard | null;
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 合并群
     */
    mergeTeams(arr1: NIMTeam[], arr2: NIMTeam | NIMTeam[]): NIMTeam[];
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 去除群
     */
    cutTeams(arr1: NIMTeam[], arr2: NIMTeam | NIMTeam[]): NIMTeam[];
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 在群数组里面根据 `teamId` 找到对应的
     *
     * @param arr1  群数组
     * @param teamId  待查找的 `teamId`
     */
    findTeam(arr1: NIMTeam[], teamId: string): NIMTeam | null;
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 去除`accounts`对应的群成员
     */
    cutTeamMembersByAccounts(arr1: NIMTeamMember[], arr2: string | string[]): NIMTeamMember[];
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 在群成员数组里面根据 `id` 找到对应的
     *
     * @param arr1  群成员数组
     * @param id  待查找的成员的 `id`
     */
    findTeamMember(arr1: NIMTeamMember[], id: string): NIMTeamMember | null;
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 合并会话
     */
    mergeSessions(arr1: NIMSession[], arr2: NIMSession | NIMSession[]): NIMSession[];
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 去除会话
     */
    cutSessions(arr1: NIMSession[], arr2: NIMSession | NIMSession[]): NIMSession[];
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 在会话数组里面根据 `id` 找到对应的
     *
     * @param arr1  会话数组
     * @param id  待查找的 `id`
     */
    findSession(arr1: NIMSession[], teamId: string): NIMSession | null;
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 合并消息
     */
    mergeMsgs(arr1: NIMMessage[], arr2: NIMMessage | NIMMessage[]): NIMMessage[];
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 去除消息
     */
    cutMsgs(arr1: NIMMessage[], arr2: NIMMessage | NIMMessage[]): NIMMessage[];
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 在消息数组里面根据 `idClient` 找到对应的
     *
     * @param arr1  消息数组
     * @param idClient  待查找的 `idClient`
     */
    findMsg(arr1: NIMMessage[], idClient: string): NIMMessage | null;
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 合并系统消息
     */
    mergeSysMsgs(arr1: NIMSystemMessage[], arr2: NIMSystemMessage | NIMSystemMessage[]): NIMSystemMessage[];
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 去除系统消息
     */
    cutSysMsgs(arr1: NIMSystemMessage[], arr2: NIMSystemMessage | NIMSystemMessage[]): NIMSystemMessage[];
    /**
     * @deprecated 废弃，合并方法参见文档：
     *
     * 在消息数组里面根据 `idServer` 找到对应的
     *
     * @param arr1  系统消息数组
     * @param idServer  待查找的 `idServer`
     */
    findSysMsg(arr1: NIMSystemMessage[], idServer: string): NIMSystemMessage | null;
    /**
     * @deprecated 由于数据库只能提供正向索引的方式查找，查找效率低，建议放弃使用
     *
     * 搜索本地会话和本地好友
     *
     * 注：该接口需要数据库支持，若没有开启本地数据库能力，done 回调里只会返回空
     */
    searchLocal(options: {
        /**
         * 搜索关键词
         */
        keyword: string;
        /**
         * 搜索路径。若不填写则搜索下面所有的字段
         */
        keyPath?: 'session' | 'session.lastMsg.type' | 'session.id' | 'session.scene' | 'session.lastMsg.type' | 'session.lastMsg.text' | 'session.localCustom' | 'session.target.account' | 'session.target.nick' | 'session.target.alias' | 'session.target.name' | 'user' | 'user.account' | 'user.nick' | 'user.alias';
        done?: (err: NIMCommonError | Error | NIMStrAnyObj | null, data: NIMSearchLocalResult) => void;
    }): void;
}
export interface NIMSearchLocalResult {
    /**
     * 匹配到的 users
     */
    users: NIMUserNameCard[];
    /**
     * 匹配到会话
     */
    sessions: NIMSession[];
    /**
     * 没匹配到的原因
     */
    msg?: string;
}
